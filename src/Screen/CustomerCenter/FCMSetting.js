import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

const FCMSetting = () => {
  return (
    <View
      style={{
        width: '100%',
        height: '100%',
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text style={{fontSize: 30}}>알림설정페이지</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default FCMSetting;
